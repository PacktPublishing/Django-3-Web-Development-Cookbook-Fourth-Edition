============
Installation
============

At the command line::

    $ easy_install django-likes

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-likes
    $ pip install django-likes
